---
title: "Developer documentation"
description: "How to authenticate to remix.world backends and which first-party API routes the web app uses."
last_verified: "2026-04-22"
---

## Audience

- **Integrators** building bots or internal tools that reuse the **Next.js BFF** routes under `https://remix.world/api/*`.
- **Backend engineers** extending the Go API hosted at `NEXT_PUBLIC_API_BASE_URL` (for example `https://dev-api.remix.world/api/v1` in tests).

This documentation **does not** expose private admin dashboards or internal-only operator APIs.

## Sections

<CardGroup cols={2}>
  <Card title="Authentication" icon="key" href="/dev/authentication">
    Dynamic JWT via `X-Authorization` headers.
  </Card>
  <Card title="API routes" icon="webhook" href="/dev/api">
    Curated list of `remix.world` `/api` routes the client calls.
  </Card>
  <Card title="Webhooks" icon="bolt" href="/dev/webhooks">
    Scope of webhook integrations (mostly internal).
  </Card>
</CardGroup>

## Source of truth

Implementation lives in the monorepo:

- Web app: `ip.world/app/api/**/route.ts`
- Shared fetchers: `ip.world/lib/client/fetcher.ts`
